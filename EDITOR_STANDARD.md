# 展位编辑器模拟版 · 20260908

## 横向工作台、Excel 与 NH · 20260910（优先于下方历史规则）

- 工作台与3D现含16个主馆及F1的NH馆；两个Mid区域仍独立保留。F1采用用户的 `F1 (7).svg`，NH保留原始地板、墙和6个门，不虚构厕所或电梯。
- “横向铺满”“旋转90°”“总图方向”只调整编辑视图；展位世界坐标、实际角度及SVG配准不变。展位本身的旋转仍在右侧属性中编辑。参考背景随画布方向一起显示。
- 左栏“Excel 信息维护”：导出当前展会、当前馆已有展位；在Excel编辑淡黄色列后，上传到同一展会与馆，检查变更预览再确认。
- 可回传展位号、企业全称、六字简称、介绍、联系人、电话、邮箱。以稳定展位ID匹配，不按行号；无ID时按当前馆展位号匹配。保留楼层、场馆、ID及表头。
- Excel不改变位置、尺寸或轮廓，不新增或删除展位；新增展位仍使用画布或SVG。空白默认不修改，明确勾选后才清空企业信息。公式需粘贴为值。
- 错馆、未知ID、重复编号、重复行及超长信息拦截整批更新；确认前不改草稿。确认后可以撤销。仅支持8MB以内.xlsx，单馆最多5000条信息。
- 数据仍保存在当前浏览器；跨设备完整备份用JSON，Excel仅交换企业信息。本次不包含云同步或正式发布功能。
- 验证：1.1馆66展位Excel导出回传，修改编号及企业信息、保留电话前导零、几何不变、撤销及重复编号拦截通过；异形绘制回归通过。NH六门识别成功，1.1馆1号门到NH1号门样例路线通过穿墙与第三馆审计；不等于所有起终点已穷举验证。

## 母版坐标修正 · 20260909-2（优先于下方旧配准规则）

- 固定关系：单馆母版 → 楼层总图内对应馆实例。上传文件是在原始单馆文件上添加展位，可有不同旋转、平移及画框范围。
- 母版库保存于 `assets/editor/hall-masters.json`，来自用户的 `Untitled (8).zip`，含16主馆及NH原始文件。当前工作台仍为16主馆。
- 导入先以固定馆内地板轮廓关联母版，再按母版到当前总图实例的刚性变换转换展位；不使用展位包围范围推算实际位置，不依赖出入口完整性或名称正确性，不改变10px=1m。
- 匹配允许起点/轮廓遍历顺序不同。每段配准最大轮廓误差≤0.05m；对称方向不唯一、缺少固定地板或形状不匹配时不猜测，提示手动核对。
- 用户应导出带原始馆内地板的完整单馆SVG，而不是只选中新增展位导出。固定结构仅用于坐标校准，不写入展位草稿。新增展位、移动展位、企业信息变更不会影响母版配准。
- 外层馆分组优先于内部旧名称，例如2.1馆内部遗留H1.1不会被误归到1.1。相似馆形状不用于猜测馆号，目标仍由工作台所选馆限定。
- 验证：16馆母版均能对应目标实例；1.1附加37°旋转、平移、改变viewBox并移除门线，66个展位世界坐标与原文件一致（误差<0.001m）。
- 新F1 (7).svg的原有8馆和Mid与当前F1一致；NH为新增，且公共地面西北轮廓改变。本次不替换主地图及道路导航源。
- NH母版和新F1内实例的变换为旋转-45°后平移(1027,2360.16) SVG单位，全部地板/墙/门顶点误差≤0.00931px。该NH画框裁剪不切割实体；此结论只适用于当前源文件，不能全局忽略裁剪。
- NH工作台入口及主地图接入另行更新，需处理6个path门、九馆材质、公共地面及导航回归；不能将当前16馆验证表述为NH已上线。

## SVG 展位输入 · 20260909-1

- 手动画图和SVG导入共用当前展会/当前馆草稿；入口“导入 SVG 展位”，先预览后确认，整批导入可撤销。
- 支持 `BOOTH__F1__H1.1__A001`、`BOOTH__F1__A001`、`BOOTH_SPECIAL/SERVICE__...`，兼容旧 `A001` 等字母加3～6位数字。未命名的图形不猜测为展位。
- 只接受展位几何；固定墙体、设施、地板不写入草稿。参考图独立，不被替换。只读取同号门用于坐标配准。
- 10 SVG单位=1米；使用至少3个同编号出入口作刚性旋转和平移配准，最大误差≤0.15m。禁止自动缩放去“塞满”展馆。旧楼层命名只提示，目标以当前馆为准；明确属于其他馆的展位跳过。
- 无可靠门配准时，整批以原比例居中作为待确认预览，用户设置中心X/Z及旋转并确认；不能把居中预览当作自动定位成功。
- 单个闭合路径、多边形、矩形、圆形、椭圆/曲线轮廓可输入；圆形保留circle，其余转换为可编辑polygon。曲线采样简化；复杂复合路径、孔洞、蒙版、裁剪需先拆分或展平，失败项目列明并跳过，不用包围矩形代替。
- 默认只追加新编号，已有编号保留；可选择更新同号几何但保留id及企业信息。不删除本次文件未出现的旧展位。文件内重复编号跳过，禁止静默覆盖。
- 超界和识别提示需明确勾选检查后才能提交；导入后仍保留编辑器墙体/房间/展位重叠检查。预览不等同于消防或布展验收。
- SVG限制12MB/2000个候选/50000节点，单馆总草稿≤5000。SVG仅按白名单重建几何测量，不运行脚本、事件、图片、外链或嵌入HTML。
- 1.1 (2).svg验证：66展位，20个门校准，90°旋转，位置与既有1.1展位数据误差<0.001m。
- 本次不改主地图、导航、设施或3D生成器；导入展位沿用草稿3D预览，未发布。

## 异形与辅助线 · 20260908-3

- 新增 circle（圆心、radius，保持真圆）、polygon（中心局部坐标 points 与旋转角）类型；无 shape 的历史数据仍按矩形解释。
- 多边形逐点绘制、首点闭合/完成按钮、撤回点、取消；可拖动顶点、选点后删点、边中点加点；拒绝自交、重复点和面积小于0.25㎡。
- 圆形圆心拖半径，圆周控制点修改半径；数值直径不变成椭圆。
- 对齐线默认开启，吸附独立开关；相邻展位包围盒边缘/中心及顶点，阈值8屏幕像素且最多1m；1m网格吸附可单独开关；Alt临时禁吸附。Shift绘制限制45°方向。
- 形状适用于选择、移动、复制、尺寸/旋转、碰撞提示、撤销、草稿、备份及3D预览。多边形宽高为本地包围尺寸，修改时缩放顶点；圆形显示真实面积。
- 3D挤出真实轮廓1.3m，不用矩形代替异形。保持固定场馆与导航页面不变。

- 独立 booth-editor.html，不修改现有前端导航、固定模型或 SVG 源文件。
- 世界坐标以米保存，与 campus-all 一致；SVG 10px=1m。16馆各自保存展位及参考图。
- 固定场馆图只读；参考图支持平移、缩放、旋转、透明度、锁定和删除，仅辅助绘制。
- 展位矩形支持画框、拖动、右下角缩放、数值调整、旋转、复制、删除、撤销及重做。
- 信息包括唯一馆内展位号、企业名称、六字简称、介绍、联系人、电话、邮箱。图片用占位符。
- 本地 IndexedDB 保存草稿；导出/导入 JSON 作跨设备备份。浏览器存储不是云端后台。
- 3D预览在独立 iframe 中叠加草稿，沿用固定模型，展位高1.3m；不发布、不修改导航碰撞数据，预览禁用导航。
- 越界、墙体/设施冲突、展位重叠提供基础几何提示；不替代消防、通道宽度与现场验收。
- 采用 UI/UX Pro Max 的标签、焦点、撤销与触控原则；沿用原生 HTML/Canvas，不迁移项目技术栈。
