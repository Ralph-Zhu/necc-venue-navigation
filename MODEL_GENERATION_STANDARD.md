# 国家会展中心 SVG → 3D 模型生成标准

**标准版本：MGS 1.0.0**  
**基准样板：7.2 馆馆内模型、3F 场馆总览**  
**适用范围：Figma 制图、SVG 导出、Web 端 2D/3D 生成、展位导入、馆内外导航**

## 1. 标准目标

同一份合格 SVG、同一份数据和同一版本生成器，在任意时间、任意设备上必须生成：

- 相同的坐标、尺寸和朝向；
- 相同的模型结构、材质和高度；
- 相同的标签显示层级；
- 相同的镜头与操作方式；
- 相同的识别结果和校验报告。

生成器不得根据画面“看起来是否合适”自动缩放单个场馆、自动猜测对象类别、随机配色、随机生成企业信息或改变对象位置。

## 2. 数据分层

模型数据必须分为三层，更新时互不覆盖：

| 数据层 | 内容 | 更新频率 | SVG 上传是否允许修改 |
|---|---|---:|---|
| 固定场馆层 | 地板、墙、门、厕所、楼梯、电梯、扶梯、服务点 | 极低 | 否，需发布新的场馆基础版本 |
| 展会布展层 | 展位轮廓、展位号、临时通道、临时服务点 | 每届展会 | 是，只替换当前展会展位层 |
| 业务数据层 | 企业名、行业、联系人、展品、Logo | 随时 | 不从 SVG 读取，由 Excel/API 按展位号匹配 |

普通“上传展位 SVG”操作只读取 `BOOTH` 和 `BOOTH_SPECIAL`，不得修改固定场馆层。

## 3. 坐标和尺寸基准

### 3.1 唯一比例

- **10 SVG 像素 = 1 米**。
- 生成比例固定为 `SCALE = 0.1`，禁止按屏幕、画板或场馆包围盒二次缩放。
- 7.2 馆基准地板 `FLOOR__F3__H7.2__MAIN`：长 **269.70 m**，宽 **106.19 m**；对应 SVG 约 **2697 × 1061.9 px**。
- SVG 尺寸误差不得超过实际尺寸的 `±0.2%`，关键门、电梯和导航连接点误差不得超过 `±0.30 m`。

### 3.2 坐标转换

以整层 SVG 的 `viewBox` 为公共坐标系：

```text
worldX = (svgX - viewBoxCenterX) / 10
worldY = 对象高度（米）
worldZ = (svgY - viewBoxCenterY) / 10
```

- SVG 左上角为二维原点，Three.js 中 SVG 向下对应世界坐标 `+Z`。
- 同一楼层所有场馆必须共享一个 viewBox 和同一原点。
- 不得对每个场馆分别居中、分别适配或分别归一化。
- 楼层之间必须使用相同的 X/Z 原点；楼层高度只由楼层配置决定。
- Figma 中禁止非等比缩放场馆组，禁止斜切。允许平移、旋转；导出前应将比例恢复为 100%。

## 4. Figma 文件结构

每个楼层使用一个顶层 Frame，场馆是其直属分组。推荐结构如下：

```text
CAMPUS__NECC
└─ FLOOR_GROUP__F3
   ├─ OUTDOOR__F3
   │  ├─ GROUND__F3
   │  ├─ ROAD_GROUP__F3
   │  └─ NAV_GROUP__F3
   ├─ HALL_GROUP__F3__H1.2
   ├─ HALL_GROUP__F3__H2.2
   ├─ ...
   └─ HALL_GROUP__F3__H7.2
      ├─ FLOOR_GROUP__F3__H7.2
      ├─ WALL_GROUP__F3__H7.2
      ├─ DOOR_GROUP__F3__H7.2
      ├─ WC_GROUP__F3__H7.2
      ├─ STAIRS_GROUP__F3__H7.2
      ├─ ELEVATOR_GROUP__F3__H7.2
      ├─ ESCALATOR_GROUP__F3__H7.2
      ├─ SERVICE_GROUP__F3__H7.2
      └─ BOOTH_GROUP__F3__H7.2
```

分组名用于整理，只有符合第 5 节命名规则的叶子对象才参与模型生成。

## 5. 对象命名标准

### 5.1 通用格式

```text
TYPE__FLOOR__HALL__OBJECT_ID
```

例如：

```text
FLOOR__F3__H7.2__MAIN
WALL__F3__H7.2__W001
DOOR__F3__H7.2__D004
WC_MALE__F3__H7.2__M01
BOOTH__F3__H7.2__A041
```

强制规则：

- 全部使用半角英文大写、数字、点和下划线；字段间只使用双下划线 `__`。
- 名称前后不得有空格，不得包含中文说明、括号、斜杠或 `#`。
- `FLOOR` 固定为 `F1/F2/F3`；`HALL` 固定为 `H1.1/H1.2/H2.1...`。
- 对象 ID 在同一场馆内必须唯一；数字序号统一补零。
- 不允许 Figma 自动产生的 `_2`、`_3` 作为正式 ID。复制对象后必须重新命名。
- `H7.2` 与 `H07.2` 不等价；只允许 `H7.2`。

### 5.2 合法类型

| 类型 | 命名示例 | Figma 图形要求 | 生成结果 |
|---|---|---|---|
| 地板 | `FLOOR__F3__H7.2__MAIN` | 单个封闭矢量 | 场馆地板 |
| 墙 | `WALL__F3__H7.2__W001` | 开放线或封闭矢量 | 实体墙段 |
| 门 | `DOOR__F3__H7.2__D004` | 单条直线 | 双开浅蓝玻璃门 |
| 男厕 | `WC_MALE__F3__H7.2__M01` | 封闭矢量 | 齐平色块 + 男厕图标 |
| 女厕 | `WC_FEMALE__F3__H7.2__F01` | 封闭矢量 | 齐平色块 + 女厕图标 |
| 无障碍卫生间 | `WC_ACCESSIBLE__F3__H7.2__A01` | 封闭矢量 | 齐平色块 + 无障碍图标 |
| 楼梯 | `STAIRS__F3__H7.2__S01` | 封闭矩形/多边形 | 七级实体楼梯 |
| 电梯 | `ELEVATOR__F3__H7.2__E01` | 封闭矩形 | 立体电梯长方体 |
| 扶梯 | `ESCALATOR__F3__H7.2__ES01` | 封闭矩形 | 带踏步和扶手的实体扶梯 |
| 服务点 | `SERVICE__F3__H7.2__SV01` | 封闭矢量 | 服务点色块 + 图标 |
| 标准展位 | `BOOTH__F3__H7.2__A041` | 封闭矩形/多边形 | 展位实体 |
| 特装展位 | `BOOTH_SPECIAL__F3__H7.2__A041` | 封闭多边形 | 特装展位实体 |
| 可步行区 | `WALKABLE__F3__H7.2__WK01` | 封闭矢量 | 导航可行走区域 |
| 禁行区 | `BLOCKED__F3__H7.2__BL01` | 封闭矢量 | 导航障碍区域 |
| 导航点 | `NAV_NODE__F3__H7.2__N001` | 小圆点 | 路网节点，不渲染 |
| 导航边 | `NAV_EDGE__F3__H7.2__N001_N002` | 单条直线 | 路网边，不渲染 |

旧文件中的 `WALLS`、`DOORS`、`ELEVATORS`、`ESCALATORS` 可由导入兼容层临时识别，但新文件和修改后的文件必须使用上表的单数类型。

### 5.3 垂直交通连接

不同楼层之间的楼梯、电梯和扶梯不得只靠坐标猜测，必须在导航数据中配置相同的 `connector_id`：

```json
{
  "connector_id": "C_H7_E01",
  "type": "elevator",
  "stops": ["F1", "F2", "F3"],
  "accessible": true
}
```

同一连接器各楼层入口的 X/Z 偏差应小于 `0.50 m`。若实际不重叠，以配置的连接关系为准。

## 6. SVG 绘制和导出要求

- 地板、厕所、展位、服务点和可步行区必须闭合。
- 门和导航边必须是独立直线，不能与墙合并成一个路径。
- 墙可以合并为一个 Compound Path 以改善性能，但不得把门、设施或展位合并进去。
- 多个独立墙段合并后必须保留所有子路径，不能栅格化。
- 设施的外框代表真实占地，不使用图标大小代表尺寸。
- Figma 颜色只用于人工区分，不作为最终材质来源。
- 禁止蒙版、模糊、阴影、图片填充、文本轮廓和不可见重复对象进入模型图层。
- 导出格式只选 SVG；不得使用“Outline text”生成无关路径。
- 导出后文件必须保留对象 `id`，且 viewBox、宽高和比例不得改变。

## 7. 固定几何生成参数

以下参数属于 MGS 1.0.0 的冻结参数，普通视觉调整不得修改：

| 对象 | 参数 |
|---|---|
| 地板 | 顶面 `Y=0`，结构厚度 `0.20 m`，向下延伸 |
| 墙 | 高 `3.60 m`；厚度取 SVG 线宽换算值，最小 `0.20 m`；底部 `Y=0` |
| 双开玻璃门 | 高 `2.70 m`、厚 `0.12 m`、左右两扇、中缝 `0.08–0.16 m` |
| 厕所地面 | 顶面 `Y=0.02 m`，视觉上与场馆地面齐平，不生成高台 |
| 电梯 | 按 SVG 占地生成，高 `3.15 m`，正面带门 |
| 楼梯 | 7 级实体，总高 `1.10 m`；朝向由长边确定 |
| 扶梯 | 12 级踏步、双侧扶手，总升高 `0.72–1.15 m` |
| 标准/特装展位 | 高 `1.45 m`，倒角 `0.055 m` |
| 导航线 | 高于地面 `0.06 m`，不得穿过展位、墙和禁行区 |

所有高度以米为单位。生成器不得按照 SVG 颜色或对象面积自行改变高度。

## 8. 统一视觉规范

### 8.1 固定色板

| 对象 | 色值 |
|---|---|
| 场景背景 | `#0B1520` |
| 室内地面 | `#586F75` |
| 墙体 | `#E5EAEC` |
| 玻璃门 | `#9BDCF0`，透明度 52% |
| 男厕 | `#6689AD` |
| 女厕 | `#B8798D` |
| 无障碍卫生间 | `#6D9A78` |
| 楼梯 | `#AA9274` |
| 扶梯 | `#C08A4D` |
| 电梯 | `#687DA8` |
| 服务点 | `#8178AA` |
| 普通展位 | `#4C9B8D` |
| 选中展位 | `#55D9BF` |

场馆总览可以为不同馆使用固定场馆色，但颜色映射必须写入配置，不得按加载顺序临时生成。Figma 中的填色不得覆盖此色板。

### 8.2 光照和渲染

- 色彩空间：sRGB。
- 色调映射：ACES Filmic。
- 抗锯齿：开启。
- 像素比：桌面端最大 `1.5`，移动端最大 `1.25`。
- 阴影默认关闭；如后续开启，只允许聚焦场馆产生阴影。
- 无动画时按需渲染，不运行永久空转的刷新循环。

## 9. 标签和图标层级

### 9.1 展位文字

- 文字浮于展位顶面之上，只显示文字，不使用铭牌背景。
- 第一行参展企业简称，最多 6 个中文字符；第二行展位号。
- 文字宽度不得超过展位屏幕投影宽度的 86%，不得溢出覆盖相邻展位。
- 选中展位文字始终显示。
- 远景只显示屏幕投影足够大的展位；拉近后逐步显示小展位。
- 企业名只能来自 Excel/API；没有数据时显示“待匹配”，不得随机生成正式数据。

### 9.2 设施图标

- 图标使用统一 24 × 24 viewBox 的线性 SVG 图标。
- 屏幕显示尺寸以 20–28 px 为限，不随模型物理尺寸放大。
- 远景隐藏全部馆内设施；聚焦场馆后才显示。
- 中景只显示电梯、扶梯、无障碍卫生间和服务点等高优先级设施。
- 近景显示全部设施，并执行屏幕碰撞避让；发生重叠时按优先级隐藏，不叠放。
- 优先级：电梯 100、扶梯 90、服务点 85、无障碍卫生间 80、楼梯 70、男女厕 50。

### 9.3 门标签

- 门统一显示“门 04”或“出入口 04”，同一页面只能选择其中一种格式。
- 总览不显示馆内门；聚焦中景优先显示 `04/07/16/19`；近景显示全部。
- 标签发生碰撞时，优先保留上述四个关键门。

## 10. 场馆总览与馆内切换

- 楼层总览必须使用整层公共坐标，不重新排布场馆。
- 拉远到总览阈值时显示全部场馆，馆内设施和展位不生成或不显示。
- 点击或拉近某个馆后，该馆保持 100% 可见，其他馆透明度固定为 `10%`。
- 聚焦后加载该馆的墙、门、设施和展位细节；退出聚焦后释放或隐藏细节对象。
- 从总览进入馆内时，相机目标移动到场馆中心，但模型世界坐标不改变。
- 馆内返回总览时恢复该楼层的标准相机位置，不根据上一次设备尺寸重算模型坐标。

## 11. 统一镜头与操作

### 11.1 操作方式

| 设备 | 操作 |
|---|---|
| 鼠标左键拖动 | 平移 |
| 鼠标右键拖动 | 旋转，不弹出右键菜单 |
| 鼠标滚轮 | 缩放 |
| 触屏单指 | 旋转 |
| 触屏双指移动 | 平移 |
| 触屏双指捏合 | 缩放 |
| `Esc` 或 `0` | 退出聚焦/返回全景 |
| 方向键 | 平移 |

不得再提供“旋转/平移模式”切换按钮。

### 11.2 相机限制

- 馆内相机视角 `FOV=42°`；楼层总览 `FOV=40°`。
- 俯仰角限制在 `0.28–1.22 rad`，禁止翻到地板下方。
- 最近距离必须保证不会穿入墙体；最远距离必须能看到整层。
- 初始朝向、初始目标、总览距离和聚焦距离必须保存为配置，不能随机或读取上一次会话状态。

## 12. 导航数据标准

导航模型与视觉模型分离：视觉墙体不能直接充当完整路网。

- 每个场馆至少包含可步行区、禁行区、门、导航节点和导航边。
- 每个展位建立一个到最近通道的目的节点，不以展位几何中心作为最终停靠点。
- 馆内到馆外通过门节点连接；馆外道路通过公共路网连接其他场馆。
- 跨层通过 `connector_id` 连接楼梯、电梯和扶梯。
- 无障碍路线排除楼梯和普通扶梯，只允许无障碍电梯及合格通道。
- 路径规划使用米作为权重；默认最短时间，可附加拥堵、封路和无障碍成本。
- 演示阶段起点可以固定为 1 号出入口；正式定位接入后只替换起点，不改变路网。

## 13. 性能上限

| 项目 | 总览目标 | 单馆聚焦目标 |
|---|---:|---:|
| Draw calls | ≤ 80 | ≤ 150 |
| 活跃 DOM 标签 | ≤ 30 | ≤ 80 |
| 同屏设施图标 | ≤ 12 | ≤ 30 |
| 移动端像素比 | ≤ 1.25 | ≤ 1.25 |
| 桌面端像素比 | ≤ 1.5 | ≤ 1.5 |
| 首次可交互 | ≤ 3 s | 细节增量加载 ≤ 1.5 s |
| 交互帧率 | ≥ 30 FPS | ≥ 30 FPS |

实现要求：

- 同材质墙体和门使用 `InstancedMesh` 或合批几何。
- 总览只加载 LOD0：地板、合并墙、合并门、场馆名。
- 聚焦馆加载 LOD1：设施、展位、图标和馆内标签。
- 非聚焦馆不得保留大量独立墙体 Mesh。
- 重复图标使用 DOM/SVG 模板复用；标签必须做距离裁剪、视锥裁剪和碰撞避让。
- 静止时停止持续渲染；只有相机、导航动画或数据变化时请求新帧。

## 14. 自动识别与校验

导入 SVG 后必须先校验，再生成模型。校验报告至少包括：

1. SVG 是否有效、是否包含 viewBox；
2. 比例是否为 10 px/m；
3. 顶层楼层和场馆分组是否存在；
4. 是否存在且只存在一个主地板；
5. ID 是否唯一、是否符合标准；
6. 是否存在前导/尾随空格、中文字符和 Figma 自动后缀；
7. 地板、设施和展位是否闭合；
8. 门是否为独立直线；
9. 展位号是否重复；
10. 固定设施是否被展位上传意外修改；
11. 垂直连接点是否有匹配的 `connector_id`；
12. 生成对象数、三角面数、Draw calls 是否超限。

严重错误必须阻止发布；警告可以预览，但必须在发布前处理。

## 15. 确定性生成规则

为保证每次结果一致，生成器必须满足：

- 对象先按 `TYPE → FLOOR → HALL → OBJECT_ID` 排序后生成。
- 场馆颜色按固定 `hall_id → color` 配置读取，不按 SVG DOM 顺序分配。
- 标签优先级和距离阈值使用固定配置，不根据本次对象数量动态变化。
- 材质、灯光、几何高度和相机参数全部来自带版本的配置文件。
- 禁止 `Math.random()` 参与正式模型、颜色、位置或企业数据生成。
- 相同输入生成相同的模型数据哈希；哈希变化必须能对应到 SVG、Excel、配置或生成器版本变化。

建议每次发布保存：

```json
{
  "standard": "MGS-1.0.0",
  "generator": "venue-web-1.0.0",
  "svg_sha256": "...",
  "business_data_sha256": "...",
  "config_sha256": "...",
  "generated_at": "2026-08-26T00:00:00+08:00"
}
```

## 16. 发布验收清单

每个新场馆或新展会发布前必须同时通过：

- [ ] 真实尺寸与 SVG 尺寸一致，比例为 10 px/m。
- [ ] 同层所有馆使用同一公共坐标和同一朝向。
- [ ] 所有正式对象 ID 符合第 5 节且全局唯一。
- [ ] 墙体与地面无缝隙，墙底为 Y=0。
- [ ] 门为双开浅蓝玻璃门且编号正确。
- [ ] 厕所地块与地面齐平；电梯、楼梯、扶梯为规定实体。
- [ ] 展位轮廓、展位号和 Excel 匹配结果一致。
- [ ] 展位文字不超出展位，不与相邻文字大面积重叠。
- [ ] 总览、聚焦、馆内和返回全景的坐标连续。
- [ ] 同馆跨层和不同馆导航可连通，无障碍路线可单独计算。
- [ ] 电脑和触屏操作符合第 11 节。
- [ ] 性能指标符合第 13 节。
- [ ] 同一输入连续生成两次，识别数量、对象坐标和模型哈希一致。

## 17. 版本和变更管理

版本号采用 `主版本.次版本.修订版本`：

- 主版本：坐标、比例、命名或模型结构发生不兼容变化；
- 次版本：新增合法对象类型、LOD 或交互能力；
- 修订版本：不改变输入输出结构的错误修复。

任何修改必须记录“修改原因、影响对象、旧值、新值、兼容方式和验收结果”。未经确认，不得修改第 3、5、7、8、9、11 节的冻结规则。

从本标准生效后，**7.2 馆是馆内模型基准样板，3F 是整层总览基准样板**。后续场馆应复用同一生成器和配置，只更换合格 SVG 与业务数据，不再为每个馆临时重写一套生成逻辑。
