# 国家会展中心 SVG → 3D 模型生成规范

**规范编号：** MGS 2.0  
**状态：** 待确认稿，尚未作为代码强制标准  
**基准案例：** 7.2 馆、F1、F3、多层视图与跨层导航  
**适用端：** 大屏、网页、H5、iPad、手机与小程序 WebView  
**编制日期：** 2026-09-04

> 本规范的首要目标是：以后更新功能时，不再破坏已经确认的模型比例、楼层位置、设施尺寸、交互方式和导航能力。凡未在变更单中明确列出的视觉差异，都视为回归问题。

---

## 0. 规范执行原则

### 0.1 固定工作流

每次更新必须依次完成：

1. 修改本规范或对应的版本化配置；
2. 校验 SVG 图层和命名；
3. 生成预览，不直接替换正式版本；
4. 与基准截图、尺寸报告、对象数量报告比较；
5. 仅在差异符合本次变更范围时发布。

禁止为了修复单个页面而临时修改全局比例、楼层高度、设施高度或相机参数。通用规则只能在配置中心修改，并提升规范版本。

### 0.2 冲突时的优先级

1. SVG 原始平面坐标和真实尺寸；
2. 墙体、门、楼层连通关系和导航安全；
3. 已经确认的模型形态和尺寸；
4. 性能与多端可用性；
5. 装饰效果。

### 0.3 三套数据必须分离

- **固定场馆数据：** 地面、墙、门、厕所、电梯、楼梯、扶梯、服务点、Mid 区；
- **展会数据：** 展位轮廓、展位号、企业信息；
- **展示状态：** 当前楼层、聚焦场馆、透明度、图标可见性、楼层爆炸间距。

切换视角只允许改变展示状态，不能重算或写回固定场馆坐标和导航坐标。

---

## 1. 坐标、比例与单位

### 1.1 唯一比例

- Figma/SVG：`10 px = 1 m`；
- Three.js：`1 world unit = 1 m`；
- 7.2 馆基准：`2697 × 1061.9 px = 269.7 × 106.19 m`；
- 所有模型禁止按页面宽度再次自适应缩放。

SVG 点 `(x, y)` 转换为世界坐标：

```text
worldX = (x - svgCenterX) / 10
worldZ = (y - svgCenterY) / 10
worldY = 对象所属楼层物理标高 + 对象局部高度
```

SVG 的 `viewBox` 只负责坐标范围，不改变上述比例。

### 1.2 物理标高与展示间距分离

- **物理标高**用于模型、路线、楼层连接器和距离计算；
- **展示间距**只用于双层总览中把楼层上下拉开；
- 单层视图中，选中楼层归一到观察平面，但其内部对象的相对高度不变；
- 导航计算永远使用物理标高，不能使用双层展示间距。

建议值，待确认：

| 参数 | 建议值 | 说明 |
|---|---:|---|
| F1 物理标高 | 0 m | 基准层 |
| F2 物理标高 | 待场馆资料确认 | 夹层预留 |
| F3 物理标高 | 待场馆资料确认 | 不凭视觉猜测 |
| 双层展示净间距 | 24 m | 可在 18–30 m 内确认 |

### 1.3 强制场景树

```text
SCENE_ROOT
├─ FLOOR_DISPLAY_ROOT__F1        # 只允许增加展示偏移
│  └─ FLOOR_PHYSICAL_ROOT__F1    # 只允许增加物理标高
│     ├─ GROUND_ROOT
│     ├─ MID_ROOT
│     ├─ HALL_ROOT
│     ├─ NAV_ROOT
│     └─ MARKER_ROOT
└─ FLOOR_DISPLAY_ROOT__F3
   └─ FLOOR_PHYSICAL_ROOT__F3
      ├─ GROUND_ROOT
      ├─ MID_ROOT
      ├─ HALL_ROOT
      ├─ NAV_ROOT
      └─ MARKER_ROOT
```

强制约束：

- 所有父子节点 `scale = (1,1,1)`；
- 场馆局部 `y = 0`，楼层高度只能由 `FLOOR_PHYSICAL_ROOT` 提供一次；
- 双层展示位移只能由 `FLOOR_DISPLAY_ROOT` 提供一次；
- 地面、模型、图标与路线必须挂在同一楼层变换链下；
- 禁止同时给父节点和子节点增加楼层高度或缩放。

这条规则用于防止“F3 场馆与地面分离”“图标游离”和“楼梯扶梯突然变得巨大”。

---

## 2. Figma 与 SVG 图层规范

### 2.1 推荐层级

```text
FLOOR__F3
├─ GROUND__F3
├─ MID__F3
├─ HALL__F3__H7.2
│  ├─ FLOOR__F3__H7.2
│  ├─ COVER__F3__H7.2
│  ├─ WALL__F3__H7.2
│  ├─ DOOR__F3__H7.2__01
│  ├─ WC_MALE__F3__H7.2__01
│  ├─ WC_FEMALE__F3__H7.2__01
│  ├─ WC_ACCESSIBLE__F3__H7.2__01
│  ├─ ELEVATOR__F3__H7.2__01
│  ├─ STAIRS__F3__H7.2__01
│  ├─ ESCALATOR__F3__H7.2__01
│  ├─ SERVICE__F3__H7.2__01
│  └─ BOOTH__F3__H7.2__7A01
├─ WALKABLE__F3
└─ BLOCKED__F3
```

统一叶节点格式：

```text
TYPE__FLOOR__HALL__ID
```

其中 `ID` 在同馆同类型内唯一。楼层公共对象可以省略 `HALL`，但不能省略 `FLOOR`。

### 2.2 导出约束

- 颜色只用于 Figma 制作时区分，不作为最终材质依据；
- 门必须独立命名，不能合并到墙体；
- 同馆同层墙体可以合并成复合路径；
- 孔洞必须使用正确填充规则导出，不能用白色图形假装孔洞；
- 不使用位图蒙版、阴影、模糊、透明叠加和剪贴蒙版表达结构；
- 所有对象保留真实 SVG 几何，禁止导出后再次拉伸；
- `COVER` 只用于远景简化，不参与碰撞和导航。

---

## 3. 模型尺寸与生成规则

以下数值为 MGS 2.0 建议基准，需在文末确认表中确认后冻结。

| 对象 | 建议高度/厚度 | 生成规则 |
|---|---:|---|
| 园区地面 GROUND | 0.15 m | 向下挤出，顶面为楼层基准面 |
| 馆内地面 FLOOR | 0.15 m | 与所属楼层顶面齐平 |
| Mid 地面 | 0.15 m | 保留真实孔洞，不能补成实心 |
| 墙 WALL | 2.40 m | 从地面顶面向上，厚度 0.20–0.28 m |
| 双开玻璃门 DOOR | 2.40 m | 浅蓝玻璃，墙上必须有真实通行缺口 |
| 厕所区域地面 | 与馆内地面齐平 | 只允许高出 0.01–0.02 m 防闪烁 |
| 电梯 ELEVATOR | 2.80 m | 立式长方体，不随楼层间距变化 |
| 楼梯 STAIRS | 总高 1.00 m | 7 级实体台阶，不承担真实跨层高度 |
| 扶梯 ESCALATOR | 总高 1.10 m | 9–12 级实体，带侧板，坡度固定 |
| 服务点 SERVICE | 1.20 m | 简化实体或图标锚点 |
| 展位 BOOTH | 1.20–1.45 m | 按展位轮廓挤出 |
| 导航线 ROUTE | 高出地面 0.06–0.10 m | 始终位于可通行地面上方 |

### 3.1 所有设施通用约束

- SVG 轮廓只决定世界坐标中的宽、深和旋转；
- 高度只能读取版本化配置，不能从 SVG 包围盒长边推导；
- 设施局部原点在自身轮廓中心，局部 `y = 0`；
- 设施不允许继承非 1 的父级缩放；
- 生成宽度和深度不得超过 SVG 原始轮廓各方向 `0.05 m`；
- 生成高度不得超过配置上限；
- 超出包围盒或高度上限时中止发布，不能只发警告后继续。

### 3.2 楼梯与扶梯专项规则

楼梯/扶梯变巨大的常见错误是：把楼层展示间距当成设施高度，或父级缩放后又按世界尺寸生成。

强制规则：

```text
generatedWidth  <= sourceFootprintWidth  + 0.05m
generatedDepth  <= sourceFootprintDepth  + 0.05m
stairsHeight    <= 1.10m
escalatorHeight <= 1.20m
parentScale     == (1,1,1)
```

- 楼梯固定 7 级；扶梯固定 9–12 级；
- 台阶沿轮廓长轴排列，不能沿世界坐标固定方向排列；
- 楼梯轮廓长边建议为 1.8–12 m，超出需人工复核；
- 扶梯轮廓长边建议为 3–20 m，超出需人工复核；
- 真正的跨层通行由导航连接器表达，不通过把楼梯模型拉到 F3 高度实现。

### 3.3 电梯专项规则

- 必须为立起的长方体；
- 高度固定为 2.80 m，不取轮廓长边；
- 电梯图标锚点在长方体顶部中心；
- 跨层连接使用相同 `connector_id` 的 F1/F3 节点；
- 展示双层时可以画跨层引导线，但不得拉伸电梯模型本体。

---

## 4. 地面、Mid、墙、门与 COVER

### 4.1 地面和 Mid

- F1、F3 均必须有完整楼层地面；
- F1 中央园区需有独立地板；
- F3 中央地板必须保留圆形孔洞；
- `MID` 是围绕孔洞的商务区，不能因解析失败而被删除；
- 孔洞范围既不渲染地板，也不生成可通行网格。

### 4.2 墙与门

- 视觉墙体和导航碰撞必须来自同一份墙体数据；
- `WALL` 所覆盖的区域不可通行；
- `DOOR` 必须在墙体碰撞中切出与门宽一致的缺口；
- 门关闭状态可作为临时阻断数据，不修改 SVG；
- 所有门至少可被选为导航终点；
- 远景仅显示关键门：`04、07、16、19`；近景显示全部门。

### 4.3 COVER

- `COVER` 只用于园区/楼层远景，展示场馆完整形状；
- 中近景必须隐藏 `COVER`，显示真实地面、墙、房间和设施；
- `COVER` 永远不参与路线计算、可通行区域生成和碰撞；
- COVER 与详细模型切换必须互斥，不能同时半透明叠加；
- 切换使用 10%–15% 的距离迟滞区，避免镜头临界位置闪烁；
- COVER 顶面与详细地面必须有至少 0.03 m 深度间隔，避免 Z-fighting。

---

## 5. 统一视觉与材质

建议默认配色：

| 对象 | 建议色 |
|---|---|
| 背景 | `#07111B` |
| 园区地面 | `#173A43` |
| 馆内地面 | `#78999D` |
| Mid 商务区 | `#A7C4C0` |
| 墙体 | `#E2EDF0` |
| 玻璃门 | `#89D9F2`，透明度 0.48 |
| 电梯 | `#6F8FC4` |
| 楼梯/扶梯 | `#C6A66A` |
| 展位 | `#6FC7B7` |
| 选中高亮 | `#42E6C4` |
| 导航主线 | `#FFD34E` |
| 非当前楼层路线 | `#8BB8FF` |

渲染约束：

- 色彩空间固定为 sRGB，色调映射配置固定；
- 默认关闭实时阴影，使用基础明暗和边缘线表达层次；
- 大屏设备像素比上限 1.5，移动端上限 1.25；
- 不因切换页面重新随机分配颜色或材质；
- 透明对象按固定层级渲染，避免不同运行次序造成显示差异。

---

## 6. LOD、图标和文字

### 6.1 四级细节

| 级别 | 使用场景 | 显示内容 |
|---|---|---|
| LOD0 | 全园区/双层总览 | 地面、Mid、COVER、馆名、导航主线 |
| LOD1 | 单层总览 | 场馆外形、主要地面、主要出入口、路线 |
| LOD2 | 场馆中景 | 真实墙体、关键门、优先设施、大型展位名 |
| LOD3 | 场馆近景 | 全部门、设施模型与图标、全部展位文字 |

阈值必须使用“相机到当前关注对象的世界距离”，不能使用页面缩放比例。进入和退出阈值不同，保留 10%–15% 迟滞。

### 6.2 图标

- 图标视觉尺寸：20–28 CSS px；
- 可点击热区：至少 44 × 44 CSS px；
- 图标使用世界锚点投影到屏幕，不给 DOM 元素累加模型位移；
- 中景最多显示 12 个高优先级图标；
- 近景最多显示 30 个图标；
- 同类密集图标执行碰撞避让，无法避让时按优先级隐藏；
- 优先级：导航终点/选中对象 > 无障碍 > 电梯 > 门 > 扶梯 > 楼梯 > 厕所 > 服务点；
- 被遮挡、在相机背后或离开视口的图标立即隐藏；
- 图标锚点必须与所属楼层使用相同的显示变换，禁止独立计算楼层偏移。

### 6.3 展位文字

- 文字浮于展位顶面，只显示文字，不显示铭牌背景；
- 第一行展位号，第二行企业简称；
- 企业简称最多 6 个汉字或等效宽度；
- 文字宽度不超过展位投影宽度的 86%；
- LOD2 只显示大型展位名称；LOD3 显示全部；
- 相邻文字重叠时，优先保留当前选中和面积较大的展位；
- 文字不得参与射线拾取，以免挡住展位点击。

---

## 7. 视角与切换规范

### 7.1 统一视图状态

系统只保留一套场景生成器和一套导航模块，状态如下：

```text
CAMPUS_ALL   双层总览
FLOOR_F1     F1 单层
FLOOR_F3     F3 单层
HALL_FOCUS   单馆聚焦
ROUTE_FOCUS  路线查看状态，不是独立页面
```

不得为导航复制一套模型或另建“导航模式”。正常浏览状态随时都能选择终点和发起导航。

### 7.2 双层总览 CAMPUS_ALL

- 同时显示 F1 与 F3，两层按确认后的展示间距分开；
- 每层的地面、Mid、场馆、路线和图标必须整体同步移动；
- 不得让场馆悬浮、地面留在原位；
- 默认使用 LOD0，只有当前路线相关设施可突破 LOD 限制显示；
- 点击楼层或场馆进入对应单层/单馆视角。

### 7.3 单层 FLOOR_F1 / FLOOR_F3

- 当前楼层显示在统一观察平面，不受其物理标高影响；
- 保留该层完整地面、Mid、孔洞和全部场馆位置；
- 单层页面必须保留完整导航模块；
- 同层导航不切换双层视图；
- 当前层初始位置默认沿用用户当前位置；没有定位时使用本层默认起点。

建议默认起点：

- F1：`1.1 馆 1 号门`；
- F3：暂定 `1.2 馆 1 号门`，待确认。

### 7.4 单馆聚焦 HALL_FOCUS

- 当前馆材质不透明度 100%；
- 同层其他馆建议不透明度 12%，且不拦截当前馆点击；
- 镜头自动适配场馆包围盒，保留 12%–18% 边距；
- 进入 LOD2，继续拉近进入 LOD3；
- 退出聚焦回到进入前的单层视角与相机位置。

### 7.5 导航触发后的自动视角

- 同馆：保持单馆或单层视图；
- 同层不同馆：保持当前单层，展示出馆—园区—目标馆入口路线；
- 不同楼层：自动切到 `CAMPUS_ALL`，同时显示两层和跨层连接；
- 到达目标层后可一键聚焦目标馆，但不强制打断总路线；
- 结束导航后保留当前视图；退出导航后回到发起前视图；
- 用户手动切换到与路线无关的楼层时，明确提示后清除路线，不能留下悬空线段。

### 7.6 相机与操作

- 推荐透视相机 FOV：40°–42°；
- 俯仰角限制：约 16°–70°；
- 禁止钻入地面、翻转场景或缩放穿透模型；
- 桌面端：左键平移、右键旋转、滚轮缩放；
- 触屏：单指旋转、双指平移、双指捏合缩放；
- 不再提供“旋转/平移”模式切换按钮；
- 点击 UI 时阻止相机手势，拖动阈值内才视为点击；
- 开启减少动态效果时，自动转场缩短为 0–150 ms。

---

## 8. 导航模型与业务规则

### 8.1 导航数据来源

- 可通行：GROUND、FLOOR、MID 中明确为通道的区域、WALKABLE；
- 不可通行：WALL、BLOCKED、展位实体、孔洞、关闭区域；
- 门：连接馆内与馆外图的边；
- 电梯、扶梯、楼梯：跨层连接器；
- COVER：完全不参与导航。

视觉模型和导航网格可以分别优化，但必须从同一份源几何生成；不得为了提高性能而忽略墙体。

### 8.2 三类导航规则

1. **同馆导航**：从当前位置沿馆内可通行区到目标；暂时无展位时，终点为所选门或设施。
2. **同层不同馆**：先到当前馆最合适的出口，再走同层园区道路，到目标馆距离目标最近的可用入口。
3. **不同楼层**：先到符合偏好的最近跨层设施，到达目标层后沿该层道路出发，再进入目标馆最近入口。

路径偏好：

- 默认：时间/距离综合最短；
- 优先电梯：提高扶梯和楼梯代价，不是禁止使用；
- 优先扶梯：提高电梯和楼梯代价；
- 优先楼梯：提高电梯和扶梯代价；
- 无障碍：禁止楼梯和普通扶梯，仅使用无障碍电梯及无障碍通道。

若首选设施不连通，允许回退并明确提示实际采用的设施。

### 8.3 跨层连接器

```json
{
  "connector_id": "ELEVATOR__EAST__01",
  "type": "elevator",
  "nodes": [
    { "floor": "F1", "x": 0, "z": 0 },
    { "floor": "F3", "x": 0, "z": 0 }
  ],
  "accessible": true,
  "status": "open"
}
```

同一实体在不同楼层必须使用相同 `connector_id`。名称接近但 ID 不同的设施不得自动连接。

---

## 9. 性能基线

| 指标 | 总览目标 | 单馆近景目标 |
|---|---:|---:|
| Draw calls | ≤ 80 | ≤ 150 |
| 同时存在的 DOM 文字 | ≤ 30 | ≤ 80 |
| 同时可见设施图标 | ≤ 12 | ≤ 30 |
| 首次可交互 | ≤ 3 s | — |
| 交互帧率 | ≥ 30 FPS | ≥ 30 FPS |

实施规则：

- 相同材质和相同结构优先使用实例化；
- 远景不生成台阶、展位文字、室内墙体等高细节网格；
- SVG 路径先做受控简化，但孔洞、门洞和拐角误差不得超过 0.05 m；
- 图标投影在相机变化时更新，不开启无意义的永久渲染循环；
- 指针移动、缩放和窗口变化使用节流；
- 隐藏楼层停止标签布局、拾取和动画计算；
- 同一份 SVG 只解析一次，生成缓存按文件哈希失效。

---

## 10. 确定性与版本化配置

同一 SVG、同一配置、同一代码版本必须生成相同结果。禁止随机颜色、随机偏移、依赖遍历顺序的材质或模型类型。

建议配置结构：

```json
{
  "standardVersion": "MGS-2.0",
  "scale": { "svgPixelsPerMeter": 10 },
  "dimensions": {
    "groundThickness": 0.15,
    "wallHeight": 2.4,
    "doorHeight": 2.4,
    "elevatorHeight": 2.8,
    "stairsSteps": 7,
    "stairsTotalHeight": 1.0,
    "escalatorSteps": 10,
    "escalatorTotalHeight": 1.1,
    "boothHeight": 1.3
  },
  "floors": {
    "F1": { "physicalElevation": 0 },
    "F2": { "physicalElevation": null },
    "F3": { "physicalElevation": null }
  },
  "display": {
    "explodedFloorGap": 24,
    "inactiveHallOpacity": 0.12,
    "mediumIconLimit": 12,
    "closeIconLimit": 30
  }
}
```

对象生成顺序按 `floor → hall → type → id` 排序。每次构建输出输入文件哈希、规范版本、配置哈希和对象统计。

---

## 11. 自动校验与阻断条件

### 11.1 导入前校验

- 图层名是否合法、对象 ID 是否重复；
- `viewBox` 与基准比例是否一致；
- FLOOR、GROUND、MID 是否存在；
- 门是否独立于墙；
- F3 中央孔洞是否存在；
- 路径是否闭合、是否存在异常极小或极大包围盒；
- 跨层设施是否有配对 `connector_id`。

### 11.2 生成后必须检查

- 楼层地面、场馆、图标和路线的变换链是否一致；
- 是否出现父子重复缩放或重复楼层偏移；
- 每类设施的最大宽、深、高是否在上限内；
- 门洞是否真的可通行；
- 墙、展位和孔洞是否进入不可通行集合；
- COVER 与详细模型是否互斥；
- 单层 F1/F3 是否都能发起导航；
- 同层路线是否保持单层，不同层路线是否自动进入双层；
- 中景图标是否仍贴附对象且无明显拥挤；
- 各端性能是否达标。

### 11.3 直接阻止发布的问题

- 任一场馆与所属地面分离；
- 任一楼层被重复抬高或重复缩放；
- 楼梯、扶梯、电梯超过配置高度或源轮廓范围；
- F3 孔洞或 Mid 区丢失；
- 路线穿墙、穿展位或跨越孔洞；
- 导航只基于 COVER；
- 单层模式没有导航模块；
- COVER 临界距离明显闪烁；
- 图标在缩放、切层时脱离锚点；
- 未经确认改变了已冻结模型尺寸、材质或操作方式。

---

## 12. 基准截图与回归测试

每次正式更新前，必须生成以下固定相机截图：

1. 7.2 馆远景 COVER；
2. 7.2 馆中景墙、门、设施；
3. 7.2 馆近景展位文字；
4. F1 单层总览及中央园区地板；
5. F3 单层总览、中央圆孔及 Mid 区；
6. F1+F3 双层总览；
7. F1 同层路线；
8. F3 同层路线；
9. F1→F3 跨层路线。

同时输出：

- 各类型对象数量；
- 各楼层和场馆包围盒；
- 地面与场馆 Y 坐标差；
- 楼梯、扶梯、电梯最大尺寸；
- 导航线与墙体碰撞次数；
- 桌面、iPad、375 px 手机宽度的帧率和可见标签数量。

除本次变更说明明确列出的项目外，基准截图和尺寸报告出现差异即视为失败。

---

## 13. 发布清单

- [ ] SVG 命名校验通过；
- [ ] 比例仍为 10 px = 1 m；
- [ ] 物理标高和展示间距完全分离；
- [ ] 父子节点没有重复缩放/位移；
- [ ] F1/F3 地面、Mid 与孔洞正确；
- [ ] 墙、门和导航碰撞一致；
- [ ] 楼梯/扶梯/电梯尺寸报告通过；
- [ ] COVER 与详细层切换无闪烁；
- [ ] 图标不游离，数量符合 LOD 上限；
- [ ] 单层、同层和跨层导航规则正确；
- [ ] 桌面和触屏操作符合规范；
- [ ] 九张基准截图回归通过；
- [ ] 旧版可回滚，配置和输入哈希已记录。

---

## 14. 待产品确认参数

请在“产品确认值”一列填写或直接修改建议值。

| 编号 | 项目 | 建议值 | 产品确认值 |
|---|---|---:|---|
| C01 | SVG 比例 | 10 px = 1 m |  |
| C02 | 墙高 | 2.40 m |  |
| C03 | 门高 | 2.40 m |  |
| C04 | 电梯模型高 | 2.80 m |  |
| C05 | 楼梯 | 7 级 / 总高 1.00 m |  |
| C06 | 扶梯 | 10 级 / 总高 1.10 m |  |
| C07 | 展位高 | 1.20–1.45 m |  |
| C08 | F1→F3 物理层高 | 待资料确认 |  |
| C09 | 双层展示间距 | 24 m |  |
| C10 | 非聚焦场馆透明度 | 12% |  |
| C11 | 远景关键门 | 04、07、16、19 |  |
| C12 | 中景最大图标数 | 12 |  |
| C13 | 近景最大图标数 | 30 |  |
| C14 | 跨层导航 | 自动切换双层 |  |
| C15 | 导航结束后的视图 | 保留当前视图 |  |
| C16 | F3 无定位时默认起点 | 1.2 馆 1 号门 |  |

---

## 15. 生效方式

本文件当前为待确认稿。完成以下步骤后才能标记为“冻结基线”：

1. C01–C16 已确认；
2. 数值写入唯一的版本化配置文件；
3. 现有 F1、F3、7.2 模型按本规范迁移；
4. 自动校验与九组基准回归全部通过；
5. 将规范状态改为 `已冻结`，并记录生效的代码版本。

冻结后，任何影响比例、模型形态、视角或导航的变更都必须先更新规范版本，再修改代码。旧版 `MODEL_GENERATION_STANDARD.md` 在此之前继续保留，避免未经确认就覆盖现有基线。
