# 2026-09-07 场景生成补充规范

## 触屏操作修订（版本 20260907-4）

- 当前 campus-all 页面：单指平移；双指捏合缩放；双指转动改变水平方向。双指不再平移，不改变俯仰角。
- 两指角度差归一到 -π～π，避免跨角度边界跳转。手指数切换沿用各触点当前坐标，整段多指操作禁止触发点击。
- 保留桌面左键平移、右键旋转（含俯仰）、滚轮缩放。模型、导航数据和比例不变。
- 此项替代 MGS 2.0 中的旧触屏约定；旧原型页不在本次改动范围。

## 手机端修订（版本 20260907-2）

- 导航面板在 <=720px 默认收起，提供展开/收起按钮；展开最大高度 65dvh，内部滚动，导航成功自动收起。
- 隐藏仅供开发参考的 GridHelper。关闭对数深度缓冲，馆内地板使用不透明材质，公共地面启用正向 polygonOffset，降低手机端叠面深度条纹风险。
- 保持所有几何尺寸和楼层展开偏移不变。手机实机横线修复效果待用户确认。

本次用户要求优先于 MGS 2.0 待确认稿的冲突项。

- 输入：F1 (5).svg、F3 (7).svg，保存于 assets/figma-f1-20260907.svg、assets/figma-f3-20260907.svg。
- 两层分别为 F1、F3，各 8 馆。中央商务区统一内部 ID 为 mid，楼层归属由父级决定。
- 以 SVG 根坐标计算，排除隐藏 SVG 的 CSS 尺寸影响；10 px = 1 m。
- 设施占地按原始几何的最小旋转包围框计算，不再限制宽深为预设尺寸，不得把对角线当宽度。
- 高度沿用原规范；楼梯 1m，扶梯踏面 1.1m，电梯 2.8m。扶梯扶手允许高于踏面约 0.25m，保持在源占地内。
- 总览以 COVER 和地面显示，Mid 同样参与细节切换。近景显示所有可见楼层的馆内细节，不再仅显示选中馆。
- 相机距离低于园区跨度 0.80 倍进入详细模式，高于 0.92 倍退出，避免临界闪烁。
- 双层展示偏移 700m，仅用于展开视图，不是真实楼层高度或导航代价。单层显示时统一归零。
- 图标沿所属楼层模型变换投影，不挪动锚点避让；重叠、屏幕外、被上层地面遮挡的图标隐藏。
- 中景最多 12 个图标，较近最多 30 个，近景取消数量上限但保留碰撞过滤；可继续放大查看密集设施。
- 同材质设施网格按区域合批，静止时停止渲染。COVER 与详细模型互斥。
- 原始文件保留不修改。子层名中的错误楼层/馆号由其所属馆组覆盖；Mid、Mid_F1 和 Floor__F3__Mid 等名称大小写兼容。
- SVG mask/defs/clipPath 中的辅助形状不直接生成模型。复合路径按 M 子路径分开，禁止跨子路径连墙。
- 现有导航为原型；真实跨层设施配对和通行状况仍须场馆数据确认，本次不把展示间距代入距离。
