# 1.1馆展位案例变更

- 仅叠加 F1 / 1.1馆展位；现有 F1/F3 地面、墙、Mid、设施和 COVER 几何不替换。
- 来源：`1.1 (2).svg`，66个闭合展位路径 A001–A066，保留原编号。
- 单馆图通过20个同号门中心与总图做刚性旋转和平移配准；不缩放，10px=1m。最大门中心残差约0.0017px。
- 高度1.3m，材质青绿色，点击高亮。归属1.1馆 detailGroup，随楼层显示变换；远景隐藏展位，近景显示。
- 顶部文字无背景，展位号+最多6字企业简称；按投影大小和屏幕碰撞隐藏小展位文字。
- 企业、联系人、电话、邮箱均模拟，不可用于真实联系。图片仅占位符。收藏存储在当前浏览器本地，不跨设备同步。
- 导航终点是展位外侧的通道点。展位多边形加入不可通行集合；馆内使用1.5m细网格，保留墙体检查；某侧不通时尝试其他展位边缘到达点。
- 导航线改为折线路径，取消可能切过障碍的曲线平滑。
- 当前Excel是与页面同源的数据清单，不包含Excel回传导入功能。

## Figma命名建议

本文件大量叶节点仍带F3，但1.1馆归属F1，本次只导入展位，不用这些叶节点替换固定设施。
建议把 `FLOOR__F3__H1.1` 改为 `FLOOR__F1__H1.1`，同馆设施中的 `__F3__` 改为 `__F1__`。
展位当前 `A001` 可识别；以后建议 `BOOTH__F1__H1.1__A001`（到A066）。本次导入脚本针对原始A编号路径，不负责通用上传。

## 验证范围

浏览器检查：66个模型和66条Excel记录；点击模型显示详情；收藏按钮状态；1.1馆1号门至所有展位的路径测试；F1/F3/双层视图；390px手机详情卡。
已执行结果：66/66条1号门至展位路线生成成功；无浏览器脚本异常；F1/F3总览各10次绘制调用，双层20次；390px手机无横向页面溢出。此为软件原型测试，不代表现场通行验收。
跨馆/跨层继续沿用已有原型，真实通道和跨层设施配对仍需场馆资料确认；本次不宣称全园区导航已验收。
