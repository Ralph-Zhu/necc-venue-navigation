# 导航规则 20260907-5

## 性能修订 20260908-1

## 卫生间连通修订 20260908-2

- 卫生间真实占地纳入所属馆的导航区域，不再因超出主展厅 FLOOR 轮廓误判为公共道路。
- 主展厅与所属卫生间接壤处允许通过无墙的实际缺口，不要求该内部入口具有展馆大门编号。仍逐段检查墙、孔洞和展位，禁止第三馆借道。
- 不修改设施模型、SVG 或图标位置；中央商务区禁穿规则不变。
- 主展厅与卫生间图形之间采用 0.8m 归属容差，兼容实测 0.3–0.5m 图层间隙；容差不取消墙体、地面和孔洞检查。
- 卫生间粗网格失败后使用 0.5m 网格重试，仍共享 20 秒预算。不同精度格点缓存隔离。
- 非凸卫生间的图标中心可能不在真实地板内；仅在此情况下选择真实卫生间内部、通过障碍检查的到达点，图标不移动，不补连中心的假直线。
- 20260908-2 实测：1.1 馆 14 个卫生间全部通过；每馆每类抽测共 48 条，41 条通过。未通过项：F1 3.1 无障碍1；F3 1.2 女厕1/男厕1/无障碍1，2.2 男厕1/无障碍1，3.2 无障碍1。其中 3.1/3.2 无障碍为预算超时。不能据此认定现场无入口。
- 手机浏览器：1.1 男厕5、2.1→1.1 女厕2、F3 6.2 无障碍1、F1→F3 6.2 女厕1均通过几何校验。跨层厕所实例约10.8秒，细网格重试仍有性能优化空间，不宣称所有厕所已连通。

- 只优化计算，保留原网格精度、墙距、门容差和最终逐段校验。
- 静态几何使用边界空间索引和精确坐标缓存；不通过坐标取整合并障碍附近的位置。
- 空闲 Worker 保留缓存；计算中取消仍销毁 Worker，拒绝旧请求结果。地图更新通过刷新页面重建缓存。
- 单层位置缓存最多 100000 项，达到上限后清空重建，限制持续使用的内存增长。


- 分类使用真实 FLOOR 轮廓；公共道路为 GROUND 扣除全部展馆和中央商务区外围包络（含外围设施实际占地）。COVER 不参与计算。
- 同馆内两点不准出馆抄近路；跨馆只允许起点馆、道路、目标馆，出起点馆后不得返回，进入目标馆后不得再离开。
- 进出展馆必须经过 SVG 门的位置；第三馆即便敞开也不能借道。
- 中央商务区外围包络为禁止穿行区（从实际地板轮廓计算，包含中间庭院）。圈外公共道路可通行。区域内设施暂提示需要补充独立入口/道路，不生成虚假接近线。
- 墙体、展位和孔洞继续阻挡；逐段验证，禁止曲线切角、跨障碍吸附及断线补直线。
- 跨层各平面遵守相同限制，公共换层设施不能成为进入无关馆的理由。既有跨层配对为演示数据，界面必须提示尚未现场确认。
- 寻路在 Worker 内执行，取消或更换目的地不接受旧结果；失败清除旧路线。
- 使用真实平面长度选择路线；楼层700m展示偏移不计入行走距离。
- 不修改 SVG、模型、材质、比例及触屏规则。

## 实现与限制

- 单一寻路引擎 `navigation-engine.js`，旧寻路与旧路线生成流程已移除。
- 1.5m细网格、A*最小堆；格点边和最终路线同时校验。门边界容差0.8m用于当前SVG门线与地板边界约0.2–0.4m偏差，不忽略真实墙体。
- 起终点仅允许3m内、连线本身合法的格点接入；最终保留原始起终点，展位例外使用已验证的外侧到达点。
- 同层选择可达路径；跨层先按偏好和距离排序，最多尝试24组符合区域限制的现有连接，非全局最优保证。计算20秒预算，超限提示，不把搜索失败当成现场绝对不可达。
- 路线按分段直线实例化绘制，不用曲线插值切角；单层/双层切换重新套用显示偏移，距离只累计平面行走段。
- 目前公共道路由楼层地面减去禁入区推导，并非现场测绘的全部开闭通道。需要以后输入显式WALKABLE/NAV网络和确认后的连接ID才能现场验收。

## 已执行验证

- 单元测试：强制馆—路—馆顺序、第三馆禁入、Mid禁入、无门不穿越、墙体/孔洞阻挡、同馆不可出馆绕行、非法最终路线被拒绝。
- 真实SVG浏览器：F1 1.1→2.1、1.1→6.1；F3 1.2→6.2；F1 1.1→F3 6.2默认/扶梯偏好，均返回已校验路线。
- 展位回归：A001、A031、A062、A066从1.1馆1号门可到达。
- 390px手机：取消后结果不回流；Mid内部终点返回未连通；F3导航切双层后目标高度700.3m（仅展示偏移）。浏览器无脚本错误。
- 触屏操作单元测试仍通过。未进行现场实测，不宣称所有设施、所有组合均可通行。
