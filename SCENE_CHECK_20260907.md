# 新地图验证记录

输入：F1 (5).svg、F3 (7).svg；入口 campus-all.html。

已验证：

- JavaScript 语法与 git diff 格式检查通过。
- 实际浏览器成功识别 16 馆、2 个 Mid、1138 个门和设施终点。
- F1/F3 单层及双层切换正常；单层楼层偏移归零，双层整体展开。
- F3 圆孔、Mid 地面和详细墙体出现在画面中；F1 中央地面保留。
- 远景详细组为 0，单层 COVER 区域 9，实测绘制调用 11。
- 拉近后同层详细组为 9、COVER 为 0；一次近景测试绘制调用 82，较远详细视角 115（取决于视野裁剪）。
- 中景 12 个图标、较近 30 个；图标占位互斥，不为了避碰偏移锚点。
- 浏览器未记录 JavaScript 错误。桌面滚轮缩放及切层已操作验证。

限制及未通过项：

- 导航回归中 F1 1.1 馆 1 号门至 2 号门返回墙体/地面边界阻挡，不能宣称新 SVG 导航已通过。当前阻挡检测没有降级为穿墙直线。
- 跨层实际连接关系未取得现场确认，未完成全路径回归。
- 尚未在真实 iPad/手机测试帧率和触屏手势；双指平移及捏合缩放已实现。
- 以上绘制调用不是 FPS，不能据此承诺所有设备流畅。
