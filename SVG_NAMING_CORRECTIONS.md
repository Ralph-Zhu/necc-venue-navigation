# F1 / F3 SVG 命名修正说明

程序已经对当前文件做容错：**设施归属以外层展馆分组为准**，因此即使 `6.1` 馆内部还写着 `H7.2`，也会先按 `6.1` 馆生成。但建议在 Figma 中修正，避免以后导入展位、跨楼层导航和数据校验时发生歧义。

## 统一规则

每个楼层的顶层分组：

- 一楼：`F1`
- 三楼：`F3`

每个展馆保持独立分组，例如 `1.1`、`7.2`。馆内直接子图层统一使用：

```text
FLOOR__{楼层}__H{馆号}
WALLS__{楼层}__H{馆号}
DOOR__{楼层}__H{馆号}
STAIRS__{楼层}__H{馆号}
ESCALATORS__{楼层}__H{馆号}
ELEVATORS__{楼层}__H{馆号}
WC_MALE__{楼层}__H{馆号}
WC_FEMALE__{楼层}__H{馆号}
WC_ACCESSIBLE__{楼层}__H{馆号}
COVER__{楼层}__H{馆号}
```

具体设施继续在末尾编号，例如：

```text
DOOR__F1__H2.1__01
ELEVATOR__F3__H7.2__03
WC_MALE__F1__H4.1__01
```

不要使用 `H04.2`、`H07.2` 这种前导零写法；统一写成 `H4.2`、`H7.2`。

## F1 (1).svg 需要修改

F1 文件中几乎所有馆内图层仍使用 `F3`，应全部改为 `F1`。

- `1.1` 馆：把所有 `__F3__H1.1` 改成 `__F1__H1.1`。
- `2.1` 馆：`DOOR/STAIRS/WC` 仍写成 `H1.1_2`；全部改成对应类型的 `__F1__H2.1`。
- `3.1` 馆：所有 `__F3__H3.2` 改成 `__F1__H3.1`。
- `4.1` 馆：所有 `H4.2/H04.2` 改成 `H4.1`，楼层改成 `F1`。
- `5.1` 馆：所有 `H5.2/H05.2` 改成 `H5.1`，楼层改成 `F1`。
- `6.1` 馆：所有 `H7.2/H07.2` 改成 `H6.1`，楼层改成 `F1`。
- `7.1` 馆：所有 `H7.2_2/H07.2_2` 改成 `H7.1`，楼层改成 `F1`。
- `8.1` 馆：所有 `H7.2_3/H07.2_3` 改成 `H8.1`，楼层改成 `F1`。
- `Mid_F1` 内的 `ELEVATORS__F3`、`STAIRS__F3`、`ESCALATORS__F3__Mid` 分别改为 `ELEVATORS__F1__MID`、`STAIRS__F1__MID`、`ESCALATORS__F1__MID`。

建议把 `Mid_F1` 保持为中央区域分组；其墙体图层改为 `WALLS__F1__MID`。

## F3 (4).svg 需要修改

- `1.2`：`WALL__H1.2` 改为 `WALLS__F3__H1.2`。
- `2.2`：`ESCALATORS__F3__H2.2_` 去掉末尾下划线；`WALL__F3__H2.2` 建议统一为 `WALLS__F3__H2.2`。
- `4.2`：`WC_MALE__F3__H04.2` 改为 `WC_MALE__F3__H4.2`。
- `5.2`：`WC_MALE__F3__H05.2` 改为 `WC_MALE__F3__H5.2`；馆内四个电梯分组错误写成 `H7.2`，改为 `H5.2`。
- `6.2`：当前多数图层错误写成 `H7.2/H07.2`，全部改为 `H6.2`。
- `7.2`：删除容器名末尾的 `_2`，并把 `H07.2` 改为 `H7.2`。
- `8.2`：所有 `H7.2_3/H07.2_3` 改为 `H8.2`。
- `Mid` 可以保留；其中图层统一使用 `__F3__MID`。

## Figma 中的操作方法

1. 在图层面板展开某个展馆，例如 `6.1`。
2. 只修改该展馆的直接子分组名称，不需要逐个修改每一条墙线。
3. 确保同一个 SVG 中 ID 唯一；不要依赖 Figma 自动追加 `_2`、`_3`。
4. 展馆复制完成后，先把外层馆号改好，再批量替换内部的楼层号和馆号。
5. 导出 SVG 后，用文本搜索检查旧馆号是否仍残留。

当前程序可以容忍上述错误，但后续做 Excel 展位匹配和跨楼层导航前，必须完成命名清理。
