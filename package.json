{
  "name": "necc-venue-navigation",
  "version": "0.1.0",
  "private": true,
  "description": "国家会展中心场馆导航与 AI 观展助手原型",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "check": "node --check server/index.js && node --check server/ai/deepseek.js && node --check server/data/exhibitors.js && node --check server/data/facility-intent.js && node --check campus-ai.js && node --check campus-all.js"
  },
  "engines": {
    "node": ">=20"
  }
}
